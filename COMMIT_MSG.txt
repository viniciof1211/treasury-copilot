feat: USD dolarization, BCCR exchange rate, detail modals & income table

- Panel Ejecutivo fully dolarized ($USD) for Board of Directors:
  - Fetches live BCCR exchange rate (tipo cambio venta) via edge function
  - Converts all CRC amounts to USD using real-time rate
  - Exchange rate badge displayed in header (e.g. CRC520.00/$1 BCCR)
  - All KPIs, charts, narratives, and labels in $ USD

- New useExchangeRate hook (src/hooks/useExchangeRate.ts):
  - Fetches BCCR USD/CRC rate with 2h localStorage cache
  - toUSD/fromUSD/normalizeCurrency/currencySymbol helpers
  - fmtCur/fmtCompact multi-currency formatters
  - Fallback to 520 CRC/USD if BCCR unavailable

- Currency defaults changed from CRC to USD across system:
  - formatCurrency/formatCompactCurrency default to USD
  - KPICard default currency USD
  - Chat agent system prompt updated to USD-first

- Per-record currency in detail views:
  - Credit operations show amounts in their original moneda (CRC/USD)
  - Cashflow ingresos table formats per-record currency
  - Gantt tooltips use operation's own currency symbol
  - Aggregated charts convert to USD for meaningful cross-currency totals

- New Ingresos detail table in Cashflow module:
  - Scrollable table with 50-row display, sticky headers
  - Columns: Operacion, Compania, Banco, Tipo, Cuota, Principal, Intereses
  - Double-click opens RecordDetailModal for view/edit

- RecordDetailModal integrated across all modules:
  - New reusable component for view/edit any record
  - CashflowDashboard: CxP + Flujo detail modals (smart type routing)
  - CreditDashboard: operations table + Gantt bars double-click
  - ComprasDashboard: Valor Nacionalizado table double-click

- Valor Nacionalizado table rows now double-clickable (ComprasDashboard)
- Credit Gantt chart bars now double-clickable with hint text
